# EPIC ACE-31: Inbound Flow PR / Branch Breakdown

เอกสารฉบับนี้เป็นแนวทางการแตกงาน (Task Breakdown) เป็น Pull Request (PR) หรือ Branch ย่อยๆ สำหรับ **Sequence 1: Inbound Flow** ของ EPIC-ACE-31 (NDP-01 ถึง NDP-05) โดยตัดส่วนของข้อมูลพื้นฐาน (Tenants, ChannelAccounts) ที่อยู่ใน EPIC-ACE-30 ออก เพื่อให้แต่ละ PR มีขนาดเล็ก รีวิวง่าย และสามารถทำงานขนานกันได้

---

## 🗄️ Stage 1: Database & Internal API (ชั้น Data สำหรับ NDP)
**โปรเจกต์ที่เกี่ยวข้อง:** `omnichat-service` และ Shared schema
**เป้าหมาย:** เตรียมตารางและ API ภายในให้พร้อมรับข้อมูลจาก Worker 

- [ ] **PR 1.1: NDP Database Schema**
    - **สิ่งที่ทำ:** สร้าง Schema/Migration เฉพาะตาราง `Contacts`, `Conversations`, `Messages`, `Attachments`, `RawEvents`
    - **จุดโฟกัสตอน Review:** Unique Constraints สำหรับทำ Idempotency (ป้องกันข้อมูลเบิ้ล) และ Indexes สำหรับทำ Cursor Pagination

- [ ] **PR 1.2: Raw Events API**
    - **สิ่งที่ทำ:** สร้าง API `POST /raw-events` (เก็บต้นฉบับตอนรับ Webhook ใหม่) และ `PATCH /raw-events/:id` (อัปเดตสถานะตอน Normalize พัง)

- [ ] **PR 1.3: Inbound Persistence API (Core Logic)**
    - **สิ่งที่ทำ:** สร้าง API `POST /messages/inbound`
    - **จุดโฟกัสตอน Review:** Logic การทำ `UPSERT` Contact ทับข้อมูลเดิม -> สร้าง/หา Conversation จาก `fallback_thread_key` -> `INSERT` Message (Idempotent)

---

## 🚪 Stage 2: Webhook Gateway (หน้าบ้านรับแขก)
**โปรเจกต์ที่เกี่ยวข้อง:** `omnichat-gateway`
**เป้าหมาย:** รับ Webhook จากแพลตฟอร์มภายนอก (LINE/FB/ฯลฯ) ตรวจสอบความถูกต้องให้เร็วที่สุด แล้วผลักลงคิวทันที

- [ ] **PR 2.1: Webhook Controller & Security**
    - **สิ่งที่ทำ:** สร้าง Controller `POST /webhooks/:channel` รวบทุก Channel
    - **จุดโฟกัสตอน Review:** Middleware ตรวจสอบ Signature (HMAC-SHA256) และ Rate Limiting

- [ ] **PR 2.2: SQS Producer & Deduplication**
    - **สิ่งที่ทำ:** เขียน Logic ดันข้อมูลลง AWS SQS FIFO
    - **จุดโฟกัสตอน Review:** การเซ็ต `MessageGroupId` (ให้คิวจาก Channel หรือ User เดียวกันเรียงลำดับถูก) และ `DeduplicationId` (`SHA256(payload)`) เพื่อกัน Webhook ถูกยิงซ้ำมา

---

## ⚙️ Stage 3: Normalization Pipeline (หัวใจการแปลงข้อมูล)
**โปรเจกต์ที่เกี่ยวข้อง:** `omnichat-normalizer-worker`
**เป้าหมาย:** ดึงคิวมาทำงาน ลบข้อมูลส่วนตัว (PII) แปลงโครงสร้างข้อมูล (Normalize) แล้วส่งไปบันทึกด้วย API ใน Stage 1

- [ ] **PR 3.1: SQS Consumer Setup & PII Redactor**
    - **สิ่งที่ทำ:** สร้างเซ็ตอัป Worker ดึงข้อมูลจาก SQS (Long-polling 20s, ทีละ 10 الرسائل) และดักลบข้อมูล PII เบื้องต้น
    - **จุดโฟกัสตอน Review:** Configuration ของ SQS Consumer

- [ ] **PR 3.2: Channel Mappers (Strategy Pattern)**
    - **สิ่งที่ทำ:** เขียน function/class แปลงข้อมูลดิบ (เช่น สร้างของ LINE ก่อน) ให้อยู่ในรูปแบบมาตรฐานของ `NormalizedEvent Contract`
    - **จุดโฟกัสตอน Review:** การใช้ Strategy Pattern เพื่อให้เพิ่ม Channel อื่นๆ ได้ง่ายในอนาคต และรองรับข้อมูล Marketplace (`source_category`: chat, order_reference, fetch_history)

- [ ] **PR 3.3: Worker Orchestration (ผูกรวม Flow)**
    - **สิ่งที่ทำ:** นำ PR 3.1 และ 3.2 มาประกอบร่าง
    - **Flow ย่อย:**
        1. เรียก PR 1.2 (Save Raw Event)
        2. พยายาม Normalize
        3. ถ้าสำเร็จ -> เรียก PR 1.3 (Save Inbound) -> Acknowledge (ลบคิว)
        4. ถ้าล้มเหลว -> เรียก PATCH แจ้ง Error -> ดำเนินการคิวต่อไป (Failure Isolation)

---

## 📎 Stage 4: Async Attachment Processing (ระบบจัดการไฟล์แนบ)
**โปรเจกต์ที่เกี่ยวข้อง:** `file-service`, `omnichat-service`, `omnichat-normalizer-worker`
**เป้าหมาย:** แยก Flow การโหลดไฟล์ภาพ/วิดีโอ/เสียง ออกมาทำทีหลังแบบ Async เพื่อไม่ให้กระทบความเร็วในการรับส่งข้อความแชทหลัก

- [ ] **PR 4.1: Internal Upload API (`file-service`)**
    - **สิ่งที่ทำ:** สร้าง `POST /files/upload` ไว้รับไฟล์ Binary เอาขึ้น S3 (Public Bucket) แล้วคืน `file_url` ที่เป็น Static URL กลับมา

- [ ] **PR 4.2: Attachment Tracking APIs (`omnichat-service`)**
    - **สิ่งที่ทำ:** สร้าง `POST /attachments` (สร้าง record สถานะ `pending`) และ `PATCH /attachments/:id` (อัปเดตไฟล์ `file_url`, ขนาด, `retry_count`, `failure_reason`, และสถานะตอนอัปโหลดเสร็จ หรือ error)

- [ ] **PR 4.3: Async Downloader (`omnichat-normalizer-worker`)**
    - **สิ่งที่ทำ:** เพิ่ม Logic ต่อจาก PR 3.3 ถ้า Message มีไฟล์แนบ ให้ยิง API โหลดไฟล์จาก Channel Network -> ตรวจสอบ Size/MIME Type -> โยนเข้า PR 4.1 -> อัปเดตสถานะด้วย PR 4.2
    - **จุดโฟกัสตอน Review:** Logic การจำกัดขนาดไฟล์ (Validation), Retry Backoff (วนซ้ำแบบถอยเวลาเมื่อเครือข่ายหลุด สูงสุด 3 ครั้ง), และการเปลี่ยนสถานะให้ถูกต้อง (`pending` -> `uploading` -> `uploaded` หรือ `failed`/`rejected`)

---

## 🔍 Stage 5: Core Query APIs (สำหรับใช้งานฝั่ง Inbox)
**โปรเจกต์ที่เกี่ยวข้อง:** `api-gateway`, `omnichat-service`
**เป้าหมาย:** สร้าง APIs สำหรับดึงข้อมูลไปแสดงผลที่หน้าจอ Inbox UI หรือนำไปให้ AI ประมวลผลต่อ

- [ ] **PR 5.1: Conversations List API**
    - **สิ่งที่ทำ:** `GET /api/v1/conversations` (ดึงรายการแชทล่าสุด)
    - **จุดโฟกัสตอน Review:** Cursor-based Pagination บนฟิลด์ `last_message_at` และการ JOIN ข้อมูลลูกค้า (Contacts)

- [ ] **PR 5.2: Message Timeline API**
    - **สิ่งที่ทำ:** `GET /api/v1/conversations/:id/messages` (ดึงประวัติในหน้าต่างแชท)
    - **จุดโฟกัสตอน Review:** Cursor-based Pagination บนฟิลด์ `created_at` และการนำ `file_url` ของ Attachments แนบกลับไปใน Response ให้อย่างถูกต้อง

---

> **💡 คำแนะนำสำหรับการทำงานคู่ขนาน (Parallel Work)**
> - ทีมพัฒนาสามารถเริ่มทำ **Stage 1 (Database & APIs)**, **Stage 2 (Webhook)**, และ **Stage 4.1 (File Upload)** ไปพร้อมๆ กันได้เลย โดยให้ทีมที่ทำ Stage 2 ลอง Mock ข้อมูลส่งเข้า SQS ไปก่อน
> - เมื่อ Stage 1 เสร็จ ทีมสามารถเดินหน้าทำ **Stage 3 (Normalizer Worker)** ต่อได้ทันที
