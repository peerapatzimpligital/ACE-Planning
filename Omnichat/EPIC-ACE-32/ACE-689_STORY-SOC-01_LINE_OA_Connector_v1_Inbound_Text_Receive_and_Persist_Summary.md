# ACE-689 STORY-SOC-01: LINE OA Connector v1 Inbound Text Receive and Persist (Summary)

**สรุป Story**
Story นี้ (ACE-689) มีเป้าหมายเพื่อนำระบบรับข้อความ (Inbound Text Event) จาก LINE Official Account แบบ End-to-End เพื่อให้สามารถใช้งาน Unified Inbox ได้ทันที โดยกระบวนการจะต้องครอบคลุมการนำข้อมูลเข้าสู่ระบบ Normalization Pipeline และเก็บบันทึกลงในฐานข้อมูล (Persistence) ตาม Schema v1 (Contact, Conversation, Message) โดยมีเงื่อนไขดังนี้:
1. **การรับข้อมูลและระบุตัวตน (Ingestion & Routing):** ต้องหาได้ว่า `tenant_id` และ `channel_account_id` คือใคร และสามารถปฏิเสธรวมถึง Log Error ได้ชัดเจน หากมีบัญชีที่ไม่รู้จักส่งมาโดยที่ระบบต้องไม่พัง (Not crash)
2. **การแปลงข้อมูล (Normalization):** ข้อมูลจะต้องถูกแปลงเป็น Schema ฝั่ง Inbound ให้มี `channel_type`: line, `direction`: inbound, `external_message_id`, `external_user_id` และ `content_text` อย่างครบถ้วน
3. **การบันทึกข้อมูล (Persistence):** ข้อมูลจะถูกบันทึกโดยการอัปเดตหรือเพิ่ม `Contact` ใหม่ สร้างหรือใช้ `Conversation` เดิม และเพิ่ม `Message` พร้อมผูกความสัมพันธ์กันให้ถูกต้อง
4. **ป้องกันข้อมูลซ้ำซ้อน (Idempotency):** หาก LINE ส่ง Webhook ซ้ำ (Retry) ระบบต้องตรวจเจอและไม่เซฟ Message เบิ้ลในระบบ
5. **การจัดการข้อผิดพลาด (Failure Handling):** หาก Database มีปัญหาชั่วคราว ต้องมีการข้ามไปก่อนและจดค่า Trace ID/Error Category อย่างปลอดภัยลง Log โดยไม่มีข้อมูลลับรั่วไหล

---

## 🏗️ การแตก Subtasks (อ้างอิงและปรับใช้ตาม Microservices ใน `ace/apps`)

**1. Infrastructure & Shared Interfaces (`packages/shared` / `omnichat-service`)**
- [ ] สรุปและอัปเดต Type/Interface ของ `LINE Inbound Webhook Event` เพื่อให้รองรับ Destination หรือ Channel Identifiers
- [ ] ยืนยัน/อัปเดตแบบแผนของ `NormalizedMessage` ฝั่ง Inbound Text ให้มีโครงสร้าง v1 ครบถ้วน (รวมถึง `external_message_id`, `external_user_id` ฯลฯ)
- [ ] สรุป/อัปเดต ER Diagram ตาม Technical Requirements ที่ระบุว่าต้องการ (สำหรับ Contact, Conversation, Message)

**2. Webhook Ingestion (`omnichat-gateway`)**
- [ ] ตรวจสอบ/แก้ไข Endpoint เดิมของ FND 03 ให้สามารถรับ LINE Message Event และรองรับกระบวนการหาค่า `tenant_id` / `channel_account_id` (Destination Mapping) ด้วย
- [ ] ดักจับเคสที่หา Destination ไม่เจอ (Unknown Destination) โดยทำการ Log เป็น Error Category อย่างชัดเจนและ Reject Event นั้นทิ้งโดยไม่ให้ Service ล่ม
- [ ] ยืนยันการส่งต่อ Event เหล่านี้เข้าคิว (Push to Queue) พร้อมส่ง Metadata ไปประมวลผลต่อ

**3. Normalization Pipeline (`omnichat-normalizer-worker`)**
- [ ] พัฒนา Queue Consumer สำหรับรับ Event LINE แบบข้อความ (Text) จากคิว
- [ ] พัฒนา Logic เพื่อดึงข้อมูล `text` จาก Event ขึ้นมา และแปลงให้อยู่ในรูปแบบ Normalized Message (ยัดค่า `channel_type: 'line'`, `direction: 'inbound'` ฯลฯ)
- [ ] ยืนยันการส่งข้อมูล (Produce Normalized Message) สู่ขั้นตอนการบันทึก (Persistence) 

**4. Persistence & Idempotency (`omnichat-service`)**
- [ ] พัฒนาหรือแก้ไข Repository ฝั่ง Contacts: ให้ทำ Contact Upsertion โดยเช็คจาก `external_user_id` ของผู้ส่ง (อัปเดตที่มีอยู่ หรือ ไม่ก็สร้างใหม่)
- [ ] พัฒนาหรือแก้ไข Repository ฝั่ง Conversations: ตรวจสอบและดึง Conversation ล่าสุด หรือสร้างใหม่พร้อมผูกกับ Contact / Channel Accounts
- [ ] พัฒนาหรือแก้ไข Repository ฝั่ง Messages: บันทึก Normalized Message ลงในตาราง ผูกสัมพันธ์ระหว่าง Contact และ Conversation
- [ ] **การทำ Idempotency:** ใส่เงื่อนไขเช็คว่ามี `external_message_id` นี้ถูกประมวลผลแล้วหรือยัง ป้องกันการบันทึกข้อความซ้ำซ้อนจาก LINE Webhook Retry
- [ ] จัดการกรณี Failure Handling: ตั้งค่า Retry / Log Exception กรณีเจอ Transient Database Error โดยใส่ Trace ID และ Error Category ครบถ้วน (แต่ไม่เผลอ Log Secret Keys ลงไป)

**5. Verification & Testing**
- [ ] เขียน/ตรวจสอบ API Spec ตาม Technical Requirements 
- [ ] เขียน Unit Tests และ Integration Tests ตั้งแต่รับ Webhook ไปจนถึง Persist ลง DB
- [ ] เขียน Tests เพื่อจำลองกรณี LINE Retry Event ซ้ำ เพื่อยืนยันว่ากลไก Idempotency ทำงานได้อย่างถูกต้อง
