# ACE-690 STORY-SOC-02: LINE OA Connector v1 Outbound Send Text and Persist (Summary)

**สรุป Story**
Story นี้ (ACE-690) มีเป้าหมายเพื่อพัฒนาระบบฝั่งส่งข้อความ (Outbound Message) กลับไปยังผู้ใช้ผ่าน LINE Official Account และบันทึกประวัติการพูดคุยลงในฐานข้อมูล เพื่อให้หน้าต่างแชท (Unified Inbox) และประวัติการ Audit มีข้อมูลที่ตรงกันและต่อเนื่อง โดยมีเงื่อนไขดังนี้:
1. **การตรวจสอบสิทธิ์ก่อนส่ง (Validation):** ระบบต้องดึงข้อมูล `tenant_id` และ `channel_account_id` ให้ถูกต้อง พร้อมกับใช้ Access Token ที่ถูกจัดเก็บไว้อย่างปลอดภัย (Credential Vault) เพื่อยิง API ของ LINE
2. **การส่งข้อความและการบันทึกข้อมูล (Sending & Persistence):** จะรองรับเฉพาะการส่งรูปแบบข้อความปกติ (Text) เท่านั้น (ยังไม่รองรับพวก Rich Menu, สติกเกอร์ ในเฟสนี้) หากส่งสำเร็จ ระบบจะสร้าง Record ในตาราง Message โดยระบุ `direction: outbound` และผูกกับ `conversation_id` เดิม
3. **การจัดการข้อผิดพลาด (Error Handling):** หากส่งข้อความไม่สำเร็จ (เช่น Token หมดอายุ หรือ Invalid) ระบบต้องบันทึกสาเหตุการเฟลด้วยหมวดหมู่ที่ชัดเจน (เช่น `auth_error`) โดยห้ามปรากฏข้อมูล Token หรือ Secret ใดๆ ลงใน Error Log เด็ดขาด
4. **ป้องกันข้อมูลซ้ำซ้อนตอน Retry (Idempotency in Retry):** หากเกิดปัญหาระหว่างส่ง เช่น Network ขัดข้อง แล้วระบบพยายามส่งใหม่ (Retry) จะต้องไม่ทำให้เกิด Message บันทึกเบิ้ลซ้ำซ้อนในฐานข้อมูล

---

## 🏗️ การแตก Subtasks (อ้างอิงและปรับใช้ตาม Microservices ใน `ace/apps`)

**1. Infrastructure & Shared Interfaces (`packages/shared` / `omnichat-service`)**
- [ ] สรุปและอัปเดต Type/Interface สำหรับการส่ง `Outbound Text Message` และหน้าตาของ Message Schema ที่มี `direction: outbound`
- [ ] เตรียมโครงสร้าง Error Category (`auth_error`, `rate_limit_error`, ฯลฯ) สำหรับกระบวนการส่งข้อความ
- [ ] สรุป/อัปเดต ER Diagram ของ Contact, Conversation, Message (ถ้ามีการเปลี่ยนโครงสร้างจาก Inbound)

**2. Core Message Sending Logic (`omnichat-service`)**
- [ ] พัฒนาฟังก์ชัน/Service ในการดึง Configuration และ Access Token ของ Tenant + Channel Account จาก Vault อย่างปลอดภัย 
- [ ] ตรวจสอบ (Validate) ว่า Conversation ID ที่จะส่งนั้น เป็นของ Tenant ที่กำลัง Request อยู่จริง และมีบริบทการคุยเป็นชอง LINE
- [ ] พัฒนา Adapter/Client สำหรับเรียก `LINE Messaging API` เพื่อส่ง Text Message (ทำแค่ Basic Rate Limit Handling เบื้องต้น)
- [ ] จัดการ Error ขาเครือข่าย หรือขาที่ LINE ตอบกลับมา (เช่น 401 Unauthorized) และแปลงให้เป็น Error Category มาตรฐาน โดยตัดทอนข้อมูลอ่อนไหวทิ้ง

**3. Data Persistence & Idempotency (`omnichat-service`)**
- [ ] พัฒนากระบวนการบันทึกฐานข้อมูล: หากส่งสำเร็จ ให้ Insert `Message` ใหม่ (ใส่ `direction: outbound`) โดยผูกกับ Conversation ID 
- [ ] พัฒนากระบวนการบันทึกฐานข้อมูล: หากส่งล้มเหลว ให้บันทึก Message หรือ Log ให้อยู่ในสถานะล้มเหลว พร้อมแนบ Error Category
- [ ] วางกลไก Idempotency (อาจจะเป็น Transaction หรือ Unique ID ตอนสั่ง Retry) เพื่อป้องกันการสร้าง Message ขาออกซ้ำซ้อนในฐานข้อมูลหากเกิด Network Transient Error

**4. Outbound API/Queue Entrypoint (`omnichat-gateway` หรือคอนซูเมอร์ของ `omnichat-service`)**
- [ ] เปิด Endpoint ภายใน (Internal API) หรือสร้าง Queue Consumer สำหรับรับคำสั่ง "Send Message" จากระบบของ Agent
- [ ] เรียกใช้งาน Core Message Sending Logic (ในข้อ 2 และ 3) เพื่อทำการต่อสายส่งออกไปยัง LINE

**5. Verification & Testing**
- [ ] เขียน/ตรวจสอบ API Spec ตาม Technical Requirements 
- [ ] เขียน Unit Tests การยิง LINE API พร้อมจำลองเคส Token Error หรือ Server Error เพื่อยืนยันว่าไม่มี Credentials หลุดรอดลง Log
- [ ] เขียน Integration Tests จำลอง Flow ทะลุตั้งแต่สั่งส่งข้อความ จนถึงบันทึกลง Database ให้ครบสถานะทั้ง Success และ Fail (รวมถึงเคส Retry)
